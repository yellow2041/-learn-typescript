// const sum = (a, b) => {
//   return a + b;
// };

// // const result=sum(10,20);
// // result.toLocaleString();

//@ts-check

/**
 *
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 */

const sum = (a, b) => {
  return a + b;
};

sum(10, "20");
